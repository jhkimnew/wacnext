How to install WAC cli of "next" version

	npm install -g windows-admin-center-cli@next

Create a new sample app named wacnext with the "next" version

	wac create --tool wacnext --company Microsoft --version next
